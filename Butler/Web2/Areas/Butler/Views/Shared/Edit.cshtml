@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>
@using (Html.BeginForm())
{
    @Html.EditorForModel()

    <input class="btn btn-primary btn-large" type="submit" value="Spara" />
    <a href="@Url.Action("Index")">Cancel</a>
}
@if (ViewBag.saved != null) {
        <div class="alert alert-success">Ändringarna är sparade</div>
}